# Módulo: com.ingsis.charstream

Provee la abstracción de lectura física y seguimiento de posición de caracteres desde streams de I/O.

## 📁 Estructura de Paquetes

* **`charstream`**: Contiene `CharStream`, `StreamCharReader`, `PositionTracker` y `CharReader`.

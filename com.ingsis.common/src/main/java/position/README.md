# Paquete: position

Provee el modelo inmutable para el rastreo de ubicación geográfica en el archivo fuente.

## 📄 Clases y Métodos

### `Position` (Record)
* `int line()`: Retorna el número de línea (1-indexed).
* `int column()`: Retorna el número de columna (1-indexed).
